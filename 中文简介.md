# DSH 维护者文档守护（v0.5.3）

---

## 它解决什么问题

在长任务里，模型最常见的失败不是"不会做"，而是**做着做着忘了自己本来要做什么**：

- 用户让它改 A，它在读 A 的过程中发现了 B，然后开始修 B；
- 一开始说的计划是对的，跳了几步之后悄悄换成了另一个目标；
- 改了"基础设施区"的文件（插件源码、配置等），却没读过同目录里已有的同类实现，又造了一个重复的东西；
- 连续调用有副作用的工具，但整轮里从没说过这一步是为了什么。

这些问题的共同点是：**每一步单独看都合规，整轮看下来却不是用户要的**。

dsh-maintainer-doc-guard 在系统提示与工具调用两处装上"护栏"，专门对付这一类失败。它的定位不是"限制模型"，而是**让模型在动手之前先想起来上下文**——大多数情况下提醒一句就够了，真正需要拒绝的很少。

---

## 核心功能（最新版能做什么）

| # | 名称 | 作用位置 | 一句话说明 |
|---|---|---|---|
| 1 | **维护文档提醒** | 系统提示（常驻段） | 每轮列出工作区的维护文档，并把 plan.md / conventions.md 的**正文**直接带上（省去每步重新打开） |
| 2 | **目标锚** | 工具调用前（追加成一条消息） | 把用户最近一条**真实输入**的原文与优先级规则（用户指令 > 模型计划 > 自发现的线索）追加在调用前；默认**不占**系统提示段，配 `anchor.delivery: 'section'` 可改回段内 |
| 3 | **先例门禁** | 工具调用前 | 改"基础设施区"（插件源码、框架代码等）前，必须读过同名已有实现，否则拒绝；同一目标最多拒 2 次后放行 |
| 4 | **意图门禁** | 工具调用前 | 本轮模型输出不足 24 字符就要调有副作用的工具时拒绝，每轮最多拦 1 次；依赖通道断裂时自我解除武装 |
| 5 | **先提醒后拦截** | 工具调用前（在 3/4 之前） | 第一次违规只提醒、放行；第二次才拒绝；同时提供"漂移检查"，连续 12 步无用户交互时注入目标复核 |
| 6 | **经验本按需命中** | 工具调用前 | `lessons.md` 不再常驻；某行的 `触发` 与本调用匹配（同工具 + 路径/命令片段命中）才把那几行注入，每条最多出一次 |
| 7 | **经验本自动归纳** | 工具返回后 | 同一目标"失败 → 再调用成功"时自动追加一行：失败的错误原文 + 改动了哪个参数键；每会话最多 3 行，shell 不计 |
| 8 | **落点核对提醒** | 工具调用前 | 一次 `edit` 让数字个数**净减少**时提醒：先判定这些数字/结论在别处是否另有落点，再让删除成立 |
| 9 | **验证读数提醒** | 工具调用前 | 写过代码类文件（`.mjs/.js/.ts/.ps1/.py/.json/.yml` 等）后，下一次调用提醒一次"验证方式 + 实际读数" |
| 10 | **侧边栏提醒开关 + 遥测** | 侧边栏 / 统计行 | 6–9 各有一个开关（进程级，默认全开）；统计行能看到各类提醒与拦截次数、以及提示段是否渲染 |

其中 3、4、5 是**同一条流水线上的三级台阶**：第 5 级先开口，第 3/4 级才动手；6–9 只观察、从不拦截。

---

## 设计亮点

- **提醒优先，拒绝为辅**：第一次只说话、放行；只有提醒被无视才拦截。跑偏多半是"忘了"，不是"对抗"，提醒一句就能拉回来。
- **基于真实事实，非模式匹配**：判据来自本次会话真实发生的事——"你真的读过先例吗？""你真的说过目的吗？""用户真的说过那句话吗？"几乎不可能误伤合规操作。
- **自我解除武装**：若依赖的通道断裂，插件停止拦截并如实显示状态，不退化成"一律拒绝"制造噪声。
- **提示段落每轮重新生成**：不累积在对话历史中，压缩后依然存在；关掉开关即刻生效，不留历史残渣。
- **默认值只收不扩**：默认永远最保守；扩大适用范围必须显式写 `true`；写错时沉默，不泄漏父级目标给子 agent。

---

## 子 Agent 支持（0.5.1 修复）

机制上插件**天生覆盖子 agent**（子 agent 继承父级预设链）。0.5.0 存在三个缺口，0.5.1 全部修复：

- **目标锚正确锚定派工单**：子 agent 只采用它看到的第一条非空用户消息（即派工单），口吻切换为"你被派的任务"，不再错误引用父级目标。
- **维护文档提醒独立开关**（`inSubagents`）：控制是否也注入子 agent。
- **目标锚独立开关**（`anchor.inSubagents`）：控制子 agent 是否锚定自己的派工单。

> 门禁（先例/意图/提醒）在子 agent 中**一直正常工作**，0.5.0 的缺陷只影响两个提示段落。

---

## 注

这是看到 B 站某个大佬的使用心得才搞的插件，很感谢那位大佬的使用心得。而且由于全是 agent 搞的，可能会遇到一些 bug 或需要改进的地方，可以说一下。

B 站 ID：**1614933063**

---

## 配置方式

### 设置页（即时生效，无需重启）

侧边栏「维护者文档」里另有**严格扣题开关**与**四类提醒开关**（经验本命中 / 删数字落点核对 / 验证读数 / 经验本自动归纳，默认全开）：
它们与下面这张卡片不同，是**进程级**的——重启回到默认，因此不占设置卡片的十字段名额。

在 **设置 → 插件 → 插件配置** 中，有 **10 个开关**（硬上限，第 11 个会让整张卡片挂不上）：

| 开关 | 默认 | 说明 |
|---|---|---|
| 先例门禁总开关 | 开 | 是否启用"改动前需读先例"的拦截 |
| 先例门禁观察模式 | 关 | 只统计"本来会拒几次"，不真的拒 |
| 同一目标最多拒几次 | 2 | 之后放行，防死磕 |
| 意图门禁总开关 | 开 | 是否启用"没说明目的就拦" |
| 意图门禁观察模式 | 关 | 只统计，不拦 |
| 每轮最多拦几次 | 1 | 防止整轮无法推进 |
| 视为"已说明"的字数下限 | 24 | 调大更严格，调小更宽松 |
| 目标锚总开关 | 开 | 是否注入"当前目标"段落 |
| 先提醒后拦截的免拦次数 | 1 | 设 0 恢复"第一次就拒" |
| 自转复核阈值（步） | 12 | 超过注入目标复核；设 0 关闭 |

### 部署配置（需重启）

通过组合配置层设置更细粒度选项（子 agent 开关、路径、文本等属于"部署决策"，故意不放设置页）：
完整字段以插件的 `Config` schema 与面板设置页为准，此处不重复。

**规则**：需要持久化的新开关**不要**加到设置页那张卡片上——该卡片有**十字段硬上限**，第 11 个字段会让整张卡挂不上。
这类开关走组合配置层，或由插件另立命名空间 / 自管文件（当前的"扣题开关"是进程级、刻意重启即丢，属于已知范围）。
