它解决什么问题

在长任务中，模型常因上下文压缩或注意力漂移而偏离用户原始目标：改错文件、重复造轮子、未说明目的就执行操作等。dsh-maintainer-doc-guard 是一个系统提示与工具调用双维度的“护栏”插件，专门对付这类失败。它不限制模型能力，而是在动手前提醒或拦截，让模型重新对齐上下文。

核心功能（五件事）
维护文档提醒：每轮在系统提示中提醒阅读项目维护文档（如 plan.md、conventions.md 等），抗上下文压缩。
目标锚（Objective Anchor）：将用户最近一条指令原文钉在系统提示末尾，明确优先级（用户指令 > 模型计划 > 模型自发现的线索），防止模型悄悄换目标。
先例门禁（Precedent Gate）：在修改“基础设施区”（如插件源码、框架代码）前，必须读过同名的已有实现，否则拒绝，避免重复造轮子。
意图门禁（Intent Gate）：调用有副作用的工具前，本轮模型必须已说明该步目的（默认至少24字符），否则拒绝。
先提醒后拦截（Nudge）：第一次违规只提醒并放行，第二次才拒绝；同时提供“自转复核”，在模型连续执行多步未与用户交互时注入目标提醒。
设计亮点
提醒优先，拒绝为辅：大多数跑偏是“忘了”，提醒一句即可拉回，避免对抗消耗预算。
基于真实事实：判据来自本次会话真实发生的事（读过、说过、用户真说过），非字符串模式匹配，几乎不误伤合规操作。
自我解除武装：若依赖的通道断裂，插件停止拦截并显示状态，不退化成“一律拒绝”。
提示段落每轮重新生成：不累积在对话历史，抗压缩，关掉即刻生效。
默认值保守：默认只在顶层生效，扩大范围需显式写 true；写错时沉默，不泄漏父级目标。
子 Agent 支持（0.5.1 修复）
机制上天生覆盖子 agent，0.5.1 修复了三个缺口：
目标锚在子 agent 中正确引用派工单而非父级目标，并采用“你被派的任务”口吻。
维护文档提醒可配置是否进入子 agent（inSubagents）。
目标锚在子 agent 中独立开关（anchor.inSubagents）。
门禁（先例/意图/提醒）在子 agent 中一直正常工作。
配置方式
设置页（即时生效）：10 个开关，包括各门禁总开关、观察模式、阈值等。
部署配置（需重启）：通过组合配置层设置更细粒度选项，如 inSubagents、anchor 文本、nudge.grace 等。
建议上手节奏：先开观察模式跑一两天，确认无误解后再正式启用。
